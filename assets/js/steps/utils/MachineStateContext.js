/**
 * @file
 * Contains MachineStateContext which is used to pass machine state to the
 * different components.
 */

import { createContext } from "react";

const MachineStateContext = createContext({});

export default MachineStateContext;
